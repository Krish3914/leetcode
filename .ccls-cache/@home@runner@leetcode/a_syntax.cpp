
static const int __ = []() {
  std::ios::sync_with_stdio(false);
  std::cin.tie(NULL);
  std::cout.tie(NULL);
  return 0;
}();






//add 2 numbers in cpp

maxPairSum = Math.Max(maxPairSum, left + right);